{
  "name": "root",
  "private": true,
  "scripts": {
    "version": "lerna version --conventional-commits --create-release github",
    "publish": "lerna publish from-git --yes"
  },
  "dependencies": {
    "lerna": "^4.0.0"
  }
}
